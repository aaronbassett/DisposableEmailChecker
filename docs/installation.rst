============
Installation
============

At the command line::

    $ easy_install DisposableEmailChecker

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv DisposableEmailChecker
    $ pip install DisposableEmailChecker
